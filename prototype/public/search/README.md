# Search Index Files

This directory will contain the generated search index files:

- `documents.json` - Flattened search documents
- `metadata.json` - Application metadata

Run `npm run build:index` to generate these files.
